# Agent 规则

## SDD 是实现门禁

`docs/SDD.md` 是项目实现前的第一依据。任何代码、接口、模块或技术选型，都必须先在 SDD 中有明确边界、契约和验收标准。

- 不得用聊天记录、临时想法或 README 替代 SDD。
- SDD 标为“待预研”的内容不得直接实现。
- 技术预研完成后先写 ADR，再同步 SDD，最后写代码。
- 发现实现与设计不一致时，先改 SDD，再改代码。

## 开始工作前

按顺序阅读：

1. `README.md`
2. `docs/SDD.md`
3. `docs/DDD.md`
4. 相关的 `docs/research/` 和 `docs/adr/` 文件

## 当前硬约束

- 第一阶段必须是 Wails macOS 应用，全盘扫描是产品目标。
- 生产版不启动本地 HTTP 服务，使用 Wails 类型化绑定和事件。
- 扫描必须支持取消、部分结果、受限并发、缓存和按需展开。
- 逻辑大小、实际占用和去重后占用不能混为一个字段。
- 清理计划必须独立、可审查，并在执行前重新校验。
- 清理即**直接删除**，不进废纸篓、不可撤销、无例外（ADR-0063，使用方三次确认）。
  唯一的拒绝是 `cleanup.DeleteBlock`（`/`、系统目录树、家目录根、卷根）——
  它保护机器不被弄坏，不保护用户不丢自己的文件。
  `recommendation.IrreplaceableReason` 只约束工具**建议**什么，不约束用户能删什么。
- AI 不能授权或执行文件操作。
- 前端不能直接访问文件系统。
- Windows 只通过平台端口预留，不进入当前实现。
- 外部开源代码必须固定版本、记录许可证和来源；禁止混入未审查的后续版本。

没有对应 SDD 条目的功能，不进入实现阶段。
