# 技术预研

技术预研用于验证 SDD 中的高风险假设。预研结论必须写入记录，并通过 ADR 才能成为实现依据。

## 当前结论

- Wails 生产版不需要独立 HTTP 控制面，采用嵌入资源、类型化绑定和应用事件。
- 成熟产品普遍采用后台扫描、渐进展示、缓存/增量刷新、受限并发和按需展开。
- Mole 的扫描实现有可借鉴的并发、缓存、去重和取消设计，但当前 JSON 不是完整目录树协议。
- 固定的 Mole `V1.40.0` 提交是 MIT，可以在保留声明和来源记录的前提下复用扫描代码；当前 `main` 不在复用范围内。
- Marmot 采用五阶段扫描、设备感知并发、有限缓存和摘要事件；结论已由 ADR-0014 接受。
- Marmot 采用 `MapQuery`/`MapResult` 的当前层渐进查询，空间聚合项不能执行文件操作；结论已由 ADR-0013 接受。
- macOS 预览使用 Quick Look、Finder 定位使用 NSWorkspace，Collector 只映射为清理计划候选；结论已由 ADR-0015 接受。
- DaisyDisk 本机实机复核确认紧凑卷入口、当前目录列表、多层 Sunburst 和底部 Collector；结论已由 ADR-0016/0017 接受。
- Windows 暂不研究实现，只保留平台抽象。

## 预研队列

### R-001 成熟产品与扫描策略

状态：已完成（产品行为和源码策略研究）

结论见 [R-001 记录](R-001-成熟产品与扫描策略.md)。

### R-002 Mole 能力与许可证

状态：已完成（本机验证和源码核对）

结论见 [R-002 记录](R-002-Mole能力与许可证.md)。

### R-003 Wails 通信与安全

状态：已完成（官方文档核对）

结论见 [R-003 记录](R-003-Wails通信与安全.md)。

### R-004 百万级节点性能与快照存储

状态：已完成（本机合成基准）

验证 SQLite 表结构、索引、批量写入、内存、懒加载查询和分页 JSON 载荷；Wails 运行时响应仍需垂直切片验证。详见 [R-004 记录](R-004-百万级节点性能与快照存储.md)。

### R-005 清理安全与恢复

状态：已完成（本机原生 API 验证；跨卷仍需 smoke test）

验证 macOS 废纸篓、计划过期、竞态、父子路径、部分失败和恢复边界。详见 [R-005 记录](R-005-清理安全与恢复.md)。

### R-006 macOS 全盘权限

状态：部分完成（ad-hoc Wails 包；真实签名/TCC 仍需发布环境）

验证 Wails 签名应用的 Full Disk Access、受限目录、系统目录、快照和管理员扫描边界。详见 [R-006 记录](R-006-macOS全盘权限.md)。

### R-007 文件系统边界与一致性

状态：已完成（macOS API 和本机样本验证）

定义符号链接、卷边界、APFS 克隆、快照、FileProvider 占位文件和扫描期间变化的领域语义。
详见 [R-007 记录](R-007-文件系统边界与一致性.md)。

### R-008 任务恢复与清理计划生命周期

状态：已完成（第一阶段语义决策；实现仍待垂直切片）

确定进程退出后的扫描任务、部分快照和清理计划生命周期。详见
[R-008 记录](R-008-任务恢复与计划生命周期.md)。

### R-009 DaisyDisk 产品体验与交互基线

状态：已完成（官网和 Version 4 用户指南黑盒核对；Marmot 实现待推进）

沉淀磁盘概览、渐进扫描、Sunburst 导航、预览、Collector、权限提示和快捷键等产品行为，
并明确 Marmot 默认移入废纸篓等安全差异。详见 [R-009 记录](R-009-DaisyDisk产品体验与交互基线.md)。

### R-010 分阶段扫描与设备感知并发

状态：已完成（本机扫描器审查、成熟产品和 Mole 策略对照；ADR 已接受）

确定五阶段扫描、设备并发预算、缓存 TTL/容量、事件预算和取消发布边界。详见
[R-010 记录](R-010-分阶段扫描与设备感知并发.md) 和 [ADR-0014](../adr/0014-分阶段扫描与设备感知并发.md)。

### R-011 Sunburst 空间图与渐进查询数据契约

状态：已完成（数据契约和载荷预算已确定；ADR 已接受）

确定 `MapQuery`/`MapResult`、真实节点与空间聚合项、当前层缓存和快照版本刷新语义。详见
[R-011 记录](R-011-Sunburst空间图与渐进查询数据契约.md) 和 [ADR-0013](../adr/0013-DaisyDisk空间图与渐进查询数据契约.md)。

### R-012 macOS 预览、Finder 定位与收集区边界

状态：已完成（macOS SDK 语法验证；原生窗口 smoke test 待实现切片；ADR 已接受）

确定 Quick Look、Finder、Collector、拖放 scope 和快照节点校验边界。详见
[R-012 记录](R-012-macOS预览Finder定位与收集区边界.md) 和 [ADR-0015](../adr/0015-macOS预览Finder定位与收集区平台边界.md)。

### R-013 DaisyDisk 原生交互与开源参考复核

状态：已完成（官方交互黑盒复核、Marmot 差距矩阵、社区项目许可证复核）

补齐悬停/焦点/选中、单击下钻、有限历史、键盘选择、虚拟空间对象和 Collector 状态机，
并核对 Radix、OpenDisk、Disk Bloom、StorageScope、Flare Scan 等 MIT 项目的固定提交和
GPL/非商用/无许可证项目的排除边界。详见 [R-013 记录](R-013-DaisyDisk原生交互与开源参考复核.md)
和 [ADR-0016](../adr/0016-DaisyDisk原生交互状态模型.md)。

### R-014 DaisyDisk 本机实机交互复核

状态：已完成（本机 DaisyDisk 可逆交互复核）

固化原版卷入口、结果工作区、单击语义、较小对象、Quick Look、导航和 Collector 行为，作为
Marmot 结果工作区重做的实测基线。详见 [R-014 记录](R-014-DaisyDisk本机实机交互复核.md)。

### R-015 有界多层空间图投影预研

状态：已完成（有界后端投影方案；ADR 已接受）

确定 Sunburst 多层 `children` 投影、深度/节点预算、聚合语义、载荷上限和降级边界。详见
[R-015 记录](R-015-有界多层空间图投影预研.md) 和 [ADR-0017](../adr/0017-有界多层空间图投影.md)。

## 记录要求

每份记录必须包含问题、假设、环境、方法、证据、结论、限制、对 DDD/SDD 的影响和建议 ADR。
